var camera, renderer, controls

function initSceneConfiguration(three_camera, three_renderer, three_controls) {
    camera = three_camera
    renderer = three_renderer
    controls = three_controls
    
}

export {
    initSceneConfiguration, camera, renderer, controls
}